"""Compatibility rules, type mapping, function mapping.

These modules are the runtime equivalent of the Markdown reference files.
Scan uses `compatibility` to classify findings; convert uses `type_mapping`
and `function_mapping` for DDL transformation.
"""
